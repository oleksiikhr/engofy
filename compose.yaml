# Pinned so `docker compose`/`make up` resolve to the same shared stack regardless of which
# worktree directory it's run from — compose otherwise defaults the project name to the cwd's
# basename, which differs per worktree and would spin up a second stack fighting over the same
# host ports (5432, 6379, ...) instead of reusing the one shared Postgres/Redis/etc.
name: engofy

services:
  postgres:
    image: postgres:18.4-alpine3.24
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/18/docker
      - ./docker/postgres-initdb.sql:/docker-entrypoint-initdb.d/postgres-initdb.sql:ro
    environment:
      POSTGRES_USER: engofy
      POSTGRES_PASSWORD: engofy
      POSTGRES_DB: engofy
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U engofy -d engofy"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:8.10.0-alpine3.23
    # Default is 16 logical DBs (0-15) — worktree-db-isolation's offset scheme
    # already uses all of them (2*N/2*N+1 for offset 0-7). Index 16 is
    # reserved for the isolated e2e stack (compose.e2e.yaml) — a future
    # widening of the offset range must not reuse it.
    command: redis-server --databases 32
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  mailhog:
    image: axllent/mailpit:v1.22.3
    ports:
      - "1025:1025"
      - "8025:8025"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8025/livez"]
      interval: 5s
      timeout: 5s
      retries: 10

  spotlight:
    image: ghcr.io/getsentry/spotlight:4.11.4
    ports:
      - "8969:8969"
    healthcheck:
      test: ["CMD-SHELL", "bash -c 'exec 3<>/dev/tcp/localhost/8969'"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
