---
slug: guest-reader-ux-overhaul
title: Гостьовий UX рідера та гачки залучення
base_branch: main
created: 2026-09-20
status: in-progress
---

# Гостьовий UX рідера та гачки залучення

## Контекст

Гостьове рев'ю сторінки статті (прихід з Google, без входу) показало: цінність продукту (клік по слову,
граматика в контексті, Analyze, Study mode, повторення) прихована, а перше враження «скучне». Цей план
закриває знайдені проблеми та додає механіки залучення.

## Продакшену ще немає

Продакшену немає, реальних користувачів і даних теж. У всіх зрізах плану:
- Зворотна сумісність не потрібна: старі поля, колонки, payload-ключі й endpoints можна змінювати або
  видаляти одразу, без expand-contract.
- Міграції не мають зберігати наявні дані. Дозволено перестворювати таблиці й перегенеровувати
  збагачення замість backfill.
- Порядок деплою і сумісність версій `apps/web` та бекенда не враховуються.
- Ризикованіші зміни припустимі: можна переписувати код, замість того щоб обходити його, і змінювати
  контракти в одному зрізі.

Виняток із contract-change rule (`task` skill, Step 2): зрізи 5, 6 і 7 змінюють контракти (колонки БД,
поля API, публічний endpoint квізу). Через відсутність продакшену кожна така зміна йде одним зрізом
без expand-contract.

Нагадування через Telegram (користувач↔чат, налаштування, cron, відписка) винесені у «Фазу 2» і в плані не мають зрізу.

## Зрізи

### [x] 1. Виправити рендер і токенізацію тексту
- Branch: `guest-reader-ux-overhaul-01-reader-render-bugs`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/107

Апостроф не відривається від слова («farmers ' market»). Прибрати порожні сині плашки після «wakes» і
«stall». «It's» не розбивається на окремі плашки. Прибрати пробіл після відкриваючих лапок
(`" You just…`).

### [x] 2. Підказка, легенда і щільність підсвічування
- Branch: `guest-reader-ux-overhaul-02-reader-onboarding-legend`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/108

Одноразова підказка «Тапни на підсвічене» і легенда базового шару (слова / фрази / граматика). Прибрати
конфлікт кольорів між базовим підсвічуванням і режимом Tenses, дати режимам зрозумілі назви або
тултіпи. Керування щільністю: за замовчуванням лише слова вище рівня читача. Власні назви («Maria») без
картки. Залежить від зрізу 1, бо змінює ті самі стилі та токени.

### [x] 3. Мобільний макет рідера
- Branch: `guest-reader-ux-overhaul-03-mobile-reader-layout`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/109

Навігація в один рядок, компактний тулбар (прокрутка або іконки), перший абзац тексту над фолдом на
390px. Скріншоти до/після в PR.

### [x] 4. Чистіші картки-підказки (лише `apps/web`)
- Branch: `guest-reader-ux-overhaul-04-popup-card-cleanup`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/110

Заголовки без ALL CAPS і без символу `|`. Людські мітки замість `PROPER_NOUN`. Назва конструкції
(«third conditional») стоїть першою. Приклад обрізається до 1–2 речень. Не змінює контракт з бекендом.

### [x] 5. Граматика для учня: пояснення і чисті приклади
- Branch: `guest-reader-ux-overhaul-05-grammar-learner-content`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/111

Нова стадія збагачення генерує для кожного граматичного пункту коротке пояснення для учня (навіщо, як
утворюється) і чисті короткі приклади замість корпусних уривків EGP. Довідник Grammar використовує це
пояснення і отримує блок «Почни звідси» за рівнем. Міграція + збагачення + `apps/web`.

### [x] 6. Український переклад слів і граматики
- Branch: `guest-reader-ux-overhaul-06-ukrainian-translations`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/112

Збагачення генерує переклад слів, фраз і граматичних пояснень українською. У popup з'являється перемикач
мови, вибір зберігається в `localStorage`. Локалізація решти інтерфейсу сайту не входить у зріз.
Міграція + збагачення + `apps/web`.

### [x] 7. Квіз без входу і збереження слів гостем
- Branch: `guest-reader-ux-overhaul-07-guest-quiz-and-saves`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/113

Виправити порожню діру в картці квізу. «Start» не веде на логін: гість проходить один раунд зі словами
цього тексту, після раунду показується CTA для реєстрації. «Add to deck» для гостя зберігається
локально й переноситься в акаунт після входу. Потрібен публічний endpoint квізу.

### [x] 8. Кінець статті: підсумок і наступний текст
- Branch: `guest-reader-ux-overhaul-08-reader-completion-next`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/114

Екран «Прочитано»: слова й граматика, які ви побачили. Наступна стаття того самого рівня. Час читання,
пояснення бейджа «B1», індикатор прогресу читання.

### [x] 9. Пошук і теми у Posts, чистка дублів
- Branch: `guest-reader-ux-overhaul-09-posts-discovery`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/115

З'ясувати, чи дублі «A Saturday Morning at the Market» і пост «Untitled» це dev-дані, чи баг інжесту
(заголовок повторено в тілі), і виправити причину. Додати пошук і теми до списку Posts.

### [x] 10. Озвучення речення й абзацу
- Branch: `guest-reader-ux-overhaul-10-read-aloud`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/116

Озвучення речення й абзацу в рідері, аудіо в граматичній картці. Спосіб (Web Speech API чи збережене
аудіо) вирішити на початку зрізу.

### [x] 11. Лічильник прогресу гостя і вибір рівня
- Branch: `guest-reader-ux-overhaul-11-guest-progress-level`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/117

Лише `apps/web`. Рідер рахує слова, які гість дослідив (localStorage), і показує «Ти дослідив N слів».
Одноразовий банер над статтею з чипами A1–C2, який можна закрити. Гість зберігає рівень у localStorage,
акаунт — через `PATCH /profile/cefr-level`. Логіка «вище рівня читача» в щільності підсвічування читає цей
рівень.

### [x] 12. Серія днів і денна ціль
- Branch: `guest-reader-ux-overhaul-12-streak-daily-goal`
- Base: `main`
- PR: https://github.com/oleksiikhr/engofy/pull/118

Денна ціль = N карток за день (за замовчуванням 10, змінюється в профілі), кільце прогресу в хедері.
Бекенд: колонка `dailyGoal` у `User` (міграція), прогрес цілі в `GET /learning/streak`, ціль у PATCH
профілю. Гість отримує серію й денний лічильник у localStorage на основі досліджених слів, без синхронізації
з акаунтом. Бекенд + `apps/web`.

### [ ] 13. Мікроанімації винагород
- Branch: `guest-reader-ux-overhaul-13-reward-animations`
- Base: `main`
- PR: —

Лише `apps/web`. Анімація завершення раунду квізу, підтвердження збереження слова, досягнення денної цілі
зі зрізу 12. Лише CSS, вимкнено при `prefers-reduced-motion`.
