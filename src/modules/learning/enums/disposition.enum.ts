// A learner's stated relationship to a target they have no active card for
// (PLAN.md, learning-foundation §2) — `Known` from "I already know this" or
// removing a graduated card; `Skipped` from an explicit "not interested".
export enum Disposition {
  Known = 'known',
  Skipped = 'skipped',
}
