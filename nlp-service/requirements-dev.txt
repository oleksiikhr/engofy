# Test-only deps. Install alongside requirements.txt:
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
# httpx (FastAPI TestClient) already comes in via uvicorn[standard].
-r requirements.txt
pytest==8.3.4
